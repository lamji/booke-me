import SignInPresentation from "@/presentations/SignIn";

/**
 * Sign In Page (Router Layer)
 *
 * No logic, no complex JSX. Only imports the Presentation.
 */
export default function SignInPage() {
    return <SignInPresentation />;
}
